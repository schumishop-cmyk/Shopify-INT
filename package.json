{
  "name": "shopify-shipping-rules",
  "version": "1.0.0",
  "description": "Advanced shipping rules integration for Shopify via Carrier Service API",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "build": "cd frontend && npm install && npm run build",
    "setup": "node scripts/setup.js",
    "register": "node scripts/register-carrier.js",
    "unregister": "node scripts/unregister-carrier.js",
    "test": "node --experimental-vm-modules node_modules/.bin/jest --runInBand",
    "test:carrier": "node scripts/test-carrier.js"
  },
  "dependencies": {
    "@shopify/shopify-api": "^11.0.0",
    "better-sqlite3": "^12.11.1",
    "cookie-parser": "^1.4.7",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "express-session": "^1.19.0",
    "winston": "^3.11.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.1.0",
    "supertest": "^7.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
