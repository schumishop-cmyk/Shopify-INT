query DeliveryInput {
  cart {
    deliveryGroups {
      id
      deliveryOptions {
        handle
        cost {
          amount
        }
      }
    }
  }
  discount {
    discountClasses
    metafield(namespace: "$app:combined-shipping", key: "config") {
      value
    }
  }
}
